import { AdbServiceBase } from "@yume-chan/adb";
import { CmdNoneProtocolService } from "./cmd.js";
import { buildArguments } from "./utils.js";
const START_ACTIVITY_OPTIONS_MAP = {
    displayId: "--display",
    windowingMode: "--windowingMode",
    forceStop: "-S",
    user: "--user",
};
export class ActivityManager extends AdbServiceBase {
    static ServiceName = "activity";
    static CommandName = "am";
    #cmd;
    constructor(adb) {
        super(adb);
        this.#cmd = new CmdNoneProtocolService(adb, ActivityManager.CommandName);
    }
    async startActivity(options) {
        let args = buildArguments([ActivityManager.ServiceName, "start-activity", "-W"], options, START_ACTIVITY_OPTIONS_MAP);
        args = args.concat(options.intent.build());
        const output = await this.#cmd
            .spawnWaitText(args)
            .then((output) => output.trim());
        for (const line of output) {
            if (line.startsWith("Error:")) {
                throw new Error(line.substring("Error:".length).trim());
            }
            if (line === "Complete") {
                return;
            }
        }
    }
}
//# sourceMappingURL=am.js.map