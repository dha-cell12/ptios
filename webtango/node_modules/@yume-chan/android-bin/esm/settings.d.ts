import type { Adb } from "@yume-chan/adb";
import { AdbServiceBase } from "@yume-chan/adb";
import type { SingleUser } from "./utils.js";
export type SettingsNamespace = "system" | "secure" | "global";
export declare const SettingsResetMode: {
    readonly UntrustedDefaults: "untrusted_defaults";
    readonly UntrustedClear: "untrusted_clear";
    readonly TrustedDefaults: "trusted_defaults";
};
export type SettingsResetMode = (typeof SettingsResetMode)[keyof typeof SettingsResetMode];
export interface SettingsOptions {
    user?: SingleUser;
}
export interface SettingsPutOptions extends SettingsOptions {
    tag?: string;
    makeDefault?: boolean;
}
export declare class Settings extends AdbServiceBase {
    #private;
    static ServiceName: string;
    static CommandName: string;
    constructor(adb: Adb);
    base(verb: string, namespace: SettingsNamespace, options: SettingsOptions | undefined, ...args: string[]): Promise<string>;
    get(namespace: SettingsNamespace, key: string, options?: SettingsOptions): Promise<string>;
    delete(namespace: SettingsNamespace, key: string, options?: SettingsOptions): Promise<void>;
    put(namespace: SettingsNamespace, key: string, value: string, options?: SettingsPutOptions): Promise<void>;
    reset(namespace: SettingsNamespace, mode: SettingsResetMode, options?: SettingsOptions): Promise<void>;
    reset(namespace: SettingsNamespace, packageName: string, tag?: string, options?: SettingsOptions): Promise<void>;
}
//# sourceMappingURL=settings.d.ts.map